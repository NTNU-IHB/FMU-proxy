
rootProject.name = 'FMU-proxy'

include 'rpc-definitions'
include 'fmu-proxy-clients'
if (getProperties().get("buildClientsOnly", false)) {
    println "Building client library only.."
} else {
    include 'fmu-proxy'
}
