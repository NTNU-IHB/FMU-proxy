
    /**
     * Autogenerated method
     */
    override fun read{{varName}}(req: Proto.UInt, responseObserver: StreamObserver<Proto.{{returnType}}> ) {

        Fmus.get(req.value)?.apply {
            val read = variableAccessor.read{{typeName}}({{valueReference}})
            responseObserver.onNext(Proto.{{returnType}}.newBuilder()
                    .setValue(read.value)
                    .setStatus(convert(read.status))
                    .build())
        }
        responseObserver.onCompleted()

    }