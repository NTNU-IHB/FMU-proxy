
    /**
     * Autogenerated method
     */
    override fun read{{varName}}(req: UIntProto, responseObserver: StreamObserver<{{returnType}}Proto> ) {

        Fmus.get(req.value)?.apply {
            val read = variableAccessor.read{{typeName}}({{valueReference}})
            responseObserver.onNext({{returnType}}Proto.newBuilder()
                    .setValue(read.value)
                    .setStatus(convert(read.status))
                    .build())
        }
        responseObserver.onCompleted()

    }